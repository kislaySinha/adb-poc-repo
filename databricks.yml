resources:
  jobs:
    hello-world-job:
      name: hello-world-job
      tasks:
        - task_key: notebook-task
          notebook_task:
            notebook_path: /Workspace/Shared/hello.py
          new_cluster:
            spark_version: 12.2.x-scala2.12
            node_type_id: Standard_DS3_v2
            num_workers: 1
